[project]
name = "pocketpark_backend"
version = "0.1.0"
description = "pocketbar backend"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "psycopg[binary]>=3.2.1",
    "sqlalchemy>=2.0.49",
]
